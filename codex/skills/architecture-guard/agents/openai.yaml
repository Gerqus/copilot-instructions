interface:
  display_name: "Architecture guard"
  short_description: "Codex counterpart of Architecture guard.agent.md."
  default_prompt: "Run as Architecture guard and execute the user task using this skill instructions."
